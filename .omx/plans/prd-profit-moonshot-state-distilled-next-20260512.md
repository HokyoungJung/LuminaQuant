# PRD — Profit Moonshot State-Distilled Non-Calendar Next Pass

Generated: 2026-05-12 KST
Repo: `/home/hoky/Quants-agent/LuminaQuant`
Context snapshot: `.omx/context/profit-moonshot-state-distilled-next-20260512T102355Z.md`

## Objective
Develop a legal, interpretable non-calendar strategy extension that explains/reproduces the rejected calendar tuple's market state without month/day/hour entry rules, then select candidates using train/validation only and expose locked-OOS only as gate/report evidence.

## Non-negotiable constraints
- Do not use fixed month/day/hour entry rules in valid candidate families.
- Treat invalid calendar/current-base tuple as a hypothesis generator only, never as a selection target.
- Preserve baseline reference `02f4520cf906f48089b8852c2651a0f1e4bd0c1c` and current green head `private/main 7e451311757a1ce0e43bebaec0a24b3746dbcb65`.
- Keep strict live promotion zero-liquidation with all split min margin buffers positive.
- Keep diagnostic nonfatal liquidation lane separate and non-promotional.
- Memory budget: < 8 GiB.

## Deliverables
1. Tests-first guardrails for non-calendar families, train/validation-only selection provenance, strict-vs-diagnostic liquidation lanes, and diagnostic liquidation metrics.
2. Narrow, interpretable candidate families:
   - crowded leadership unwind v2,
   - funding/OI exhaustion carry reversal,
   - beta-hedged residual reversion,
   - dispersion compression breakout/unwind,
   - volatility/regime exposure scaling.
3. Replay artifact under `var/reports/.../alpha_v2/state_distilled_*`.
4. Liquidation-aware strict/diagnostic artifact under `var/reports/.../alpha_v2/liquidation_aware_state_distilled_*`.
5. Handoff docs and `.omx/notepad.md` update.
6. Required verification outputs, Lore commit, push to `private/main`, GitHub Actions `ci`/`private-ci` green.

## Acceptance criteria
- Calendar-primary strategy use is blocked.
- Selection provenance states `selection_inputs=[train, validation]` and `uses_locked_oos_for_selection=false`.
- Train and validation returns positive for selected candidate.
- Strict lane: train/validation/OOS liquidation count = 0 and all split min margin buffers > 0.
- OOS MDD <= 25%.
- OOS return and return/MDD beat baseline/current-base reference.
- Sharpe, Sortino, smart Sortino, and Calmar are acceptable.
- Diagnostic nonfatal lane reports liquidation count, event drawdown, equity loss, and recovery semantics separately from promotion.
- Full required test/lint/compile/diff verification passes.

## RALPLAN iteration 2 hardening updates
- Factory-wide valid-family allowlist must be tested: every new valid family generated by `_candidate_specs` must have empty calendar month/day/hour entry fields and zero calendar veto fields unless explicitly marked research-only/calendar-primary.
- Calendar-proxy robustness must be recorded before any promotion claim:
  - month-label invariance/unit placebo for representative signals,
  - blocked-time/date-block concentration diagnostics in the report artifact,
  - calendar-label permutation or explicit reason why a lightweight equivalent was used,
  - rolling/walk-forward status field, even if no promotion candidate emerges.
- Future holdout policy: because locked-OOS has been repeatedly observed during this research loop, any live-promotion claim must state that this locked-OOS is gate/report evidence only and that a fresh future holdout is required before real deployment.
- Explicit risk-adjusted metric gates: OOS MDD <= 25%, OOS Sharpe >= 1.0, Sortino >= 1.0, smart Sortino >= 1.0, Calmar >= 1.0, and OOS return plus return/MDD must exceed the current-base reference.
- Staffing/verification: at least one lane must be dedicated to verification/provenance, not only implementation.
