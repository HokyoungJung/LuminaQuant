# LuminaQuant Repository Layout (2026-06-10, Phase 7 — Final)

Preferred structure (high level):

```text
LuminaQuant/
├─ README.md
├─ README_KR.md
├─ pyproject.toml
├─ config.yaml
├─ configs/
│  ├─ score_config.example.json
│  ├─ config.example.yaml
│  └─ profiles/
├─ src/
│  └─ lumina_quant/
│     ├─ cli/
│     ├─ workflows/
│     ├─ backtesting/
│     ├─ live/
│     ├─ optimization/
│     ├─ strategies/
│     ├─ indicators/
│     ├─ data/
│     └─ storage/
│        ├─ wal/
│        ├─ parquet/
│        └─ postgres/
├─ native/
│  └─ lumina_compute/      ← pyo3 Rust extension (maturin; replaces 5 ctypes loaders)
├─ apps/
│  └─ dashboard_web/       ← Next.js 14 app (Phase 6; replaces Streamlit dashboard)
│     ├─ app/
│     ├─ components/
│     └─ lib/
├─ scripts/
│  ├─ ci/
│  ├─ ops/
│  ├─ dev/
│  └─ research/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ fixtures/
└─ var/
   ├─ data/
   ├─ logs/
   ├─ reports/
   ├─ optimized_params/
   └─ cache/
```

Notes:
- Use `uv run lq ...` as the primary CLI.
- Root compatibility shims were removed; use `uv run lq ...` entrypoints only.
- Runtime artifacts should prefer `var/`.

---

<!-- LQ:TREE:START -->
## Bounded Repo Tree / Ownership Map (autogenerated 2026-06-10, Phase 7 — Final)

Regenerate after broad file moves or ownership changes. Keep this section bounded; do **not** paste `var/reports`, `var/cache`, `.git`, virtualenvs, pycache, generated logs, or full artifact trees.

Suggested refresh command:

```sh
python3 scripts/dev/generate_agents_tree.py  # if absent, use find/tree with the exclusions above and update only this marker block
```

### Bounded tree (portfolio/optimization-relevant)

```text
src/lumina_quant/
    src/lumina_quant/alpha_zoo/
    src/lumina_quant/backtesting/
    src/lumina_quant/calibration/
    src/lumina_quant/cli/
    src/lumina_quant/compute/
    src/lumina_quant/configuration/
    src/lumina_quant/core/
    src/lumina_quant/dashboard/
    src/lumina_quant/data/
      src/lumina_quant/data/loaders/
    src/lumina_quant/eval/
    src/lumina_quant/exchanges/
    src/lumina_quant/indicators/
      src/lumina_quant/indicators/alpha101/
    src/lumina_quant/infra/
    src/lumina_quant/live/
    src/lumina_quant/optimization/
    src/lumina_quant/portfolio/
    src/lumina_quant/services/
    src/lumina_quant/storage/
      src/lumina_quant/storage/parquet/
      src/lumina_quant/storage/postgres/
      src/lumina_quant/storage/wal/
    src/lumina_quant/strategies/
      src/lumina_quant/strategies/plugins/
    src/lumina_quant/strategy_factory/
    src/lumina_quant/tuning/
    src/lumina_quant/utils/
    src/lumina_quant/workflows/
  src/lumina_quant/portfolio_followup_rules.py
  src/lumina_quant/portfolio_split_contract.py
    src/lumina_quant/backtesting/portfolio_backtest.py
    src/lumina_quant/calibration/__init__.py
    src/lumina_quant/calibration/cost_calibration.py
    src/lumina_quant/cli/optimize.py
    src/lumina_quant/compute/__init__.py
    src/lumina_quant/compute/indicators.py
    src/lumina_quant/compute/ohlcv_loader.py
    src/lumina_quant/compute/ops.py
    src/lumina_quant/configuration/validate.py
    src/lumina_quant/core/__init__.py
    src/lumina_quant/core/engine.py
    src/lumina_quant/core/events.py
    src/lumina_quant/core/market_window_contract.py
    src/lumina_quant/core/memory_budget.py
    src/lumina_quant/core/plugin_registry.py
    src/lumina_quant/core/protocols.py
    src/lumina_quant/core/session_memory.py
    src/lumina_quant/core/strategy_input.py
    src/lumina_quant/dashboard/__init__.py
    src/lumina_quant/dashboard/bridge.py
    src/lumina_quant/dashboard/cutover_surfaces_service.py
    src/lumina_quant/dashboard/exact_window_bundle.py
    src/lumina_quant/dashboard/exact_window_service.py
    src/lumina_quant/dashboard/overview_service.py
    src/lumina_quant/dashboard/risk_health_service.py
    src/lumina_quant/dashboard/state_store_service.py
    src/lumina_quant/dashboard/workflow_jobs_service.py
    src/lumina_quant/data/__init__.py
    src/lumina_quant/data/feature_points.py
    src/lumina_quant/data/hyperliquid_readonly.py
    src/lumina_quant/data/native_raw_first_backend.py
    src/lumina_quant/data/ohlcv.py
    src/lumina_quant/data/raw_first_lineage.py
    src/lumina_quant/data/support_inventory.py
    src/lumina_quant/data/symbols.py
    src/lumina_quant/data/timeframe.py
      src/lumina_quant/data/loaders/__init__.py
      src/lumina_quant/data/loaders/ohlcv_loader.py
    src/lumina_quant/alpha_zoo/crypto_fx_factors.py
    src/lumina_quant/alpha_zoo/factor_card.py
    src/lumina_quant/alpha_zoo/operators.py
    src/lumina_quant/alpha_zoo/optuna_hybrid_config.py
    src/lumina_quant/alpha_zoo/optuna_hybrid_live_strategy.py
    src/lumina_quant/alpha_zoo/optuna_hybrid_signals.py
    src/lumina_quant/eval/exact_window_decision.py
    src/lumina_quant/exchanges/__init__.py
    src/lumina_quant/exchanges/binance_futures_client.py
    src/lumina_quant/exchanges/binance_futures_exchange.py
    src/lumina_quant/exchanges/mt5_exchange.py
    src/lumina_quant/exchanges/polymarket_exchange.py
    src/lumina_quant/indicators/oscillators.py
      src/lumina_quant/indicators/alpha101/__init__.py
      src/lumina_quant/indicators/alpha101/compiler.py
      src/lumina_quant/indicators/alpha101/formula_ir.py
      src/lumina_quant/indicators/alpha101/formula_sources.py
      src/lumina_quant/indicators/alpha101/registry.py
    src/lumina_quant/infra/__init__.py
    src/lumina_quant/live/portfolio.py
    src/lumina_quant/live/recovery_reconciliation.py
    src/lumina_quant/optimization/__init__.py
    src/lumina_quant/optimization/constants.py
    src/lumina_quant/optimization/fast_eval.py
    src/lumina_quant/optimization/frozen_dataset.py
    src/lumina_quant/optimization/native_backend.py
    src/lumina_quant/optimization/search_policy.py
    src/lumina_quant/optimization/storage.py
    src/lumina_quant/optimization/threading_control.py
    src/lumina_quant/optimization/walkers.py
    src/lumina_quant/portfolio/__init__.py
    src/lumina_quant/portfolio/cost_aware_constructor.py
    src/lumina_quant/services/portfolio.py
    src/lumina_quant/storage/__init__.py
      src/lumina_quant/storage/parquet/__init__.py
      src/lumina_quant/storage/parquet/compaction.py
      src/lumina_quant/storage/parquet/materialized.py
      src/lumina_quant/storage/parquet/ohlcv_repo.py
      src/lumina_quant/storage/parquet/raw_aggtrades.py
      src/lumina_quant/storage/postgres/__init__.py
      src/lumina_quant/storage/postgres/state_repo.py
      src/lumina_quant/storage/wal/__init__.py
      src/lumina_quant/storage/wal/binary.py
      src/lumina_quant/storage/wal/native_backend.py
    src/lumina_quant/strategies/artifact_portfolio_mode.py
    src/lumina_quant/strategies/profit_moonshot.py
    src/lumina_quant/strategies/profit_moonshot_breakout.py
    src/lumina_quant/strategies/profit_moonshot_reversion.py
    src/lumina_quant/strategies/profit_moonshot_trend.py
      src/lumina_quant/strategies/plugins/__init__.py
      src/lumina_quant/strategies/plugins/trend_momentum.py
      src/lumina_quant/strategies/plugins/xs_mean_reversion.py
    src/lumina_quant/strategy_factory/__init__.py
    src/lumina_quant/strategy_factory/candidate_library.py
    src/lumina_quant/strategy_factory/pipeline.py
    src/lumina_quant/strategy_factory/research_entrypoints.py
    src/lumina_quant/strategy_factory/research_metrics.py
    src/lumina_quant/strategy_factory/research_reporting.py
    src/lumina_quant/strategy_factory/research_resources.py
    src/lumina_quant/strategy_factory/research_run_support.py
    src/lumina_quant/strategy_factory/research_runner.py
    src/lumina_quant/strategy_factory/research_stage_selection.py
    src/lumina_quant/strategy_factory/research_stage_support.py
    src/lumina_quant/strategy_factory/runtime_settings.py
    src/lumina_quant/strategy_factory/selection.py
    src/lumina_quant/strategy_factory/strategy_signal_dispatch.py
    src/lumina_quant/tuning/__init__.py
    src/lumina_quant/tuning/param_registry.py
    src/lumina_quant/utils/__init__.py
    src/lumina_quant/utils/audit_store.py
    src/lumina_quant/utils/logging_utils.py
    src/lumina_quant/utils/notification.py
    src/lumina_quant/utils/performance.py
    src/lumina_quant/utils/persistence.py
    src/lumina_quant/utils/report_generator.py
    src/lumina_quant/utils/risk_free.py
    src/lumina_quant/workflows/autonomous_portfolio_research_loop.py
    src/lumina_quant/workflows/optimize_workflow.py
scripts/
    scripts/ci/
    scripts/dev/
    scripts/ops/
    scripts/research/
  scripts/benchmark_optimization_kernel.py
  scripts/build_live_portfolio_dashboard_summary.py
  scripts/resume_autonomous_portfolio_research.sh
  scripts/run_autonomous_portfolio_research_loop.py
  scripts/run_portfolio_optimization.py
    scripts/ci/architecture_gate_binance_native.sh
    scripts/ci/architecture_gate_live_data.sh
    scripts/ci/architecture_gate_market_window_contract.sh
    scripts/ci/check_market_window_rollout_gates.py
    scripts/ci/export_market_window_gate_metrics.py
    scripts/ci/verify_polars_gpu_runtime.py
    scripts/dev/relaunch_autonomous_team.sh
    scripts/dev/resume_autonomous_team.py
    scripts/ops/inspect_autoresearch_run.py
    scripts/ops/install_shell_aliases.sh
    scripts/ops/live_readiness_preflight.py
    scripts/ops/request_live_stop.py
    scripts/ops/start_live_session.sh
    scripts/ops/stop_live_session.sh
    scripts/ops/summarize_fill_slippage.py
    scripts/research/analyze_hybrid_live_investability.py
    scripts/research/build_committee_portfolio_followup.py
    scripts/research/build_portfolio_exact_window_freeze.py
    scripts/research/build_portfolio_one_shot_incumbent_bundle.py
    scripts/research/build_production_guarded_portfolio.py
    scripts/research/materialize_legacy_metric_hybrid.py
    scripts/research/optuna_tune_hybrid_online_portfolio.py
    scripts/research/optuna_tune_profit_moonshot_calendar.py
    scripts/research/pair_spread_4h_xpt_xpd_retune.py
    scripts/research/profit_moonshot_research.py
    scripts/research/refresh_final_portfolio_validation_data.py
    scripts/research/refresh_final_portfolio_validation_data_fast.sh
    scripts/research/replay_profit_moonshot_fresh_start.py
    scripts/research/retune_live_portfolio_hybrid.py
    scripts/research/revalidate_live_equivalent_candidates.py
    scripts/research/run_carry_trend_production_retune.py
    scripts/research/run_causal_dynamic_portfolio.py
    scripts/research/run_causal_overlay_portfolio.py
    scripts/research/run_four_sleeve_regime_portfolio.py
    scripts/research/run_grouped_allocator_strict_leverage_batch_retune.py
    scripts/research/run_hybrid_online_portfolio.py
    scripts/research/run_hybrid_online_portfolio_major.py
    scripts/research/run_profit_moonshot_cadence_sweep.py
    scripts/research/run_regime_switching_portfolio.py
      ... (14 more selected files omitted)
tests/
    tests/fixtures/
    tests/integration/
    tests/unit/
  tests/test_autonomous_portfolio_research_loop.py
  tests/test_backtest_optimize_loader_entrypoint.py
  tests/test_build_committee_portfolio_followup.py
  tests/test_build_portfolio_exact_window_freeze.py
  tests/test_build_portfolio_one_shot_incumbent_bundle.py
  tests/test_build_production_guarded_portfolio_script.py
  tests/test_causal_dynamic_portfolio.py
  tests/test_causal_overlay_portfolio.py
  tests/test_exact_window_decision.py
  tests/test_hybrid_online_portfolio.py
  tests/test_live_execution_reconciliation.py
  tests/test_live_portfolio_boundary.py
  tests/test_live_recovery_reconciliation_service.py
  tests/test_optimize_cli_runtime_env.py
  tests/test_optimize_data_mode_contract.py
  tests/test_optimize_import_side_effects.py
  tests/test_optimize_two_stage.py
  tests/test_optimize_uses_windowed_handler.py
  tests/test_optuna_tune_profit_moonshot_calendar.py
  tests/test_pair_spread_4h_xpt_xpd_retune.py
  tests/test_portfolio_fast_stats.py
  tests/test_portfolio_followup_memory_guard.py
  tests/test_portfolio_followup_rules.py
  tests/test_portfolio_max_performance_decision_meta.py
    ... (19 more selected files omitted)
    tests/unit/test_analyze_hybrid_live_investability.py
    tests/unit/test_artifact_portfolio_mode.py
    tests/unit/test_backtest_live_portfolio_mode_resolution.py
    tests/unit/test_grouped_allocator_strict_leverage_batch_retune.py
    tests/unit/test_portfolio_component_positions.py
    tests/unit/test_portfolio_master_scoreboard.py
    tests/unit/test_portfolio_operating_playbook.py
    tests/unit/test_portfolio_operating_switch.py
    tests/unit/test_portfolio_split_contract_env_override.py
    tests/unit/test_profit_moonshot_cadence_sweep.py
    tests/unit/test_profit_moonshot_research.py
    tests/unit/test_profit_moonshot_strategies.py
    tests/unit/test_write_portfolio_live_readiness_decision.py
docs/
    docs/kr/
    docs/live-readiness/
  docs/OPTIMIZATION_REFACTOR_NOTES.md
  docs/PORTFOLIO_8GB_PUSH_READINESS_CHECKLIST.md
  docs/hybrid_live_investability_20260417.md
  docs/performance_first_threshold_coverage_adjusted_retune_20260417.md
  docs/session_handoff_20260408_article_pipeline.md
  docs/session_handoff_20260409_article_pipeline_reboot.md
  docs/session_handoff_20260411_article_pipeline_complete_and_pair_followup.md
  docs/session_handoff_20260411_article_pipeline_continue.md
  docs/session_handoff_20260411_article_pipeline_post_timeout_wave.md
  docs/session_handoff_20260411_wal_bottleneck_mitigated.md
  docs/session_handoff_20260412_broader_redesign_and_ralph_audit.md
  docs/session_handoff_20260412_pair_spread_robustness_continuation_results.md
  docs/session_handoff_20260412_pair_spread_robustness_next.md
  docs/session_handoff_20260413_risk_off_switch_and_bearish_followup.md
  docs/session_handoff_20260414_hybrid_reboot_2025_2026_split.md
  docs/session_handoff_20260416_hybrid_perf_replay_ready.md
  docs/session_handoff_20260416_performance_first_switch_override.md
  docs/session_handoff_20260416_reboot_validation_rerun.md
  docs/session_handoff_20260418T102013Z_carry_trend_retune_resume.md
  docs/session_handoff_20260418T155907Z_carry_trend_full_confirmation.md
  docs/session_handoff_20260507_portfolio_optimizer_integration_cleanup.md
  docs/session_handoff_20260508_portfolio_optimizer_integration_cleanup.md
    docs/kr/API.md
    docs/kr/ARCHITECTURE_PATTERN_BENCHMARK_ADOPTION.md
    docs/kr/DESIGN_NOTES_GPU_AUTO.md
    docs/kr/EXCHANGES.md
    docs/kr/EXTERNAL_DATA.md
    docs/kr/FUTURES_STRATEGY_FACTORY.md
    docs/kr/METRICS.md
    docs/kr/MIGRATION_GUIDE_POSTGRES_PARQUET.md
    docs/kr/MINIMAL_INSTALLS.md
    docs/kr/QUICKSTART_8GB_BASELINE.md
    docs/kr/RUNBOOK_1Y_1S_LOCAL.md
    docs/kr/SCORING_CONFIG_GUIDE.md
    docs/kr/TRADING_MANUAL.md
    docs/kr/WORKFLOW.md
    docs/live-readiness/03-aggressive-sleeve-local-retune-plan.md
.github/
    .github/ISSUE_TEMPLATE/
    .github/workflows/
  .github/PULL_REQUEST_TEMPLATE.md
  .github/dependabot.yml
  .github/hardcoded_params_baseline.json
    .github/ISSUE_TEMPLATE/bug_report.yml
    .github/ISSUE_TEMPLATE/config.yml
    .github/ISSUE_TEMPLATE/feature_request.yml
    .github/workflows/ci.yml
    .github/workflows/private-ci.yml
.omx/plans/
  .omx/plans/cleanup-repo-wide-audit-live-backtest-ci-gpu.md
  .omx/plans/dynamic-causal-portfolio-allocator-2026-03-14.md
  .omx/plans/exact-window-low-ram-backtest-portfolio-ralplan.md
  .omx/plans/live_equivalent_5symbol_optimization_then_resume_20260429.md
  .omx/plans/portfolio-superiority-next-broad-wave-20260422T1950KST.md
  .omx/plans/portfolio-superiority-shadow-lane-ralplan.md
  .omx/plans/prd-adaptive-online-portfolio-governor-8gb.md
  .omx/plans/prd-autonomous-portfolio-research-loop.md
  .omx/plans/prd-final-portfolio-validation-latest-tail.md
  .omx/plans/prd-luminaquant-portfolio-superiority.md
  .omx/plans/prd-portfolio-four-sleeve-retune.md
  .omx/plans/prd-portfolio-max-performance-loop.md
  .omx/plans/prd-portfolio-superiority-8gb-moonshot.md
  .omx/plans/prd-portfolio-superiority-new-methods-8gb.md
  .omx/plans/prd-profit-moonshot-pass-under-8gb-20260507.md
  .omx/plans/prd-profit-moonshot-strategy-factory-20260501.md
  .omx/plans/prd-repo-wide-audit-live-backtest-ci-gpu.md
  .omx/plans/prd-strict-portfolio-validation-optimization-live-readiness.md
  .omx/plans/profit-moonshot-multiasset-exchange-expansion-20260506.md
  .omx/plans/quants-agent-audit-execution-plan.md
  .omx/plans/ralplan-deliberate-portfolio-max-performance-loop-2026-03-14.md
  .omx/plans/ralplan-deliberate-portfolio-superiority-8gb-moonshot-2026-04-02.md
  .omx/plans/ralplan-deliberate-portfolio-walk-forward-exec-2026-03-14.md
  .omx/plans/ralplan-deliberate-strict-portfolio-validation-optimization-live-readiness-2026-03-19.md
      ... (27 more selected files omitted)
```

### Ownership map

- **Portfolio optimizer shared core**: `src/lumina_quant/portfolio/` owns reusable portfolio stream/split/constraint/allocation helpers. Keep script CLIs as thin compatibility wrappers around shared code.
- **Generic portfolio optimizer CLI**: `scripts/run_portfolio_optimization.py` owns user-facing arguments, report writing, memory-guard lifecycle, and compatibility output schema.
- **Research tuners and Optuna lanes**: `scripts/research/*tune*portfolio*.py` and `scripts/research/optuna_tune_*.py` own experiment orchestration. Selection objectives must declare whether they are `train_val_only_locked_oos_report` or diagnostic; OOS is not selection authority unless explicitly diagnostic-labeled. New tunable search loops should use `src/lumina_quant/optimization/search_policy.py` instead of open-coding Optuna study creation or unbounded cartesian grids.
- **Validator / evidence gates**: `scripts/research/validate_*` and `src/lumina_quant/portfolio_split_contract.py` own RSS/CI/git evidence parsing and pass/fail reports. Preserve `<8GiB` memory checks.
- **Core optimization engine**: `src/lumina_quant/optimization/`, `src/lumina_quant/cli/optimize.py`, and `src/lumina_quant/tuning/` own strategy parameter optimization, Optuna/grid execution, walk-forward windows, storage, and runtime threading. `optimization/search_policy.py` is the shared boundary for Optuna study execution, bounded-grid metadata, and locked-OOS non-use flags.
- **Tests**: `tests/test_run_portfolio_optimization_script.py`, `tests/test_profit_moonshot_fresh_portfolio_tuning.py`, `tests/test_optuna_tune_profit_moonshot_calendar.py`, `tests/test_profit_moonshot_pass_under_8gb_validator.py`, and new `tests/test_portfolio_optimizer_core.py` lock behavior before cleanup.
- **Docs / handoff**: `docs/session_handoff_*`, `.omx/plans/*`, and `.omx/notepad.md` hold reboot-safe state. Commit concise handoffs; avoid raw report payloads.
- **Artifacts**: `var/reports/` contains generated evidence; commit only intentional concise PASS/readiness artifacts. `var/cache/` remains ignored.

### Phase 1 architecture notes (2026-06-10)

- **Unified config entry-point**: `lumina_quant.configuration` is the ONLY config entry-point. `runtime_access.py` and `config.py` have been deleted. All 18 operational scripts migrated to `lumina_quant.configuration` directly. Never add a second global config loader.
- **Config engine views**: `configuration/views.py` was deleted in Phase 6 (deletion gate closed). `BacktestConfigView` now lives in `backtesting/_config_view.py` (permanent engine-private adapter) and is re-exported from `lumina_quant.configuration` for script compat. `LiveConfigView` was inlined as `_build_live_config_namespace()` in `live/trader.py`. New code must import from `lumina_quant.configuration` (schema types) or `lumina_quant.backtesting._config_view` (engine adapter). Never create a new views.py.
- **Plugin registry**: `lumina_quant.core.plugin_registry` owns the `@register(kind, name, *, interface=None)` decorator and the `GLOBAL_REGISTRY` accessor. Kinds: `"strategy"`, `"indicator"`, `"portfolio"`. `cli/_strategy_registry_fallback.py` has been deleted; `PublicStubStrategy` / `PublicStrategyRegistry` / `load_strategy_registry` now live in `core/plugin_registry`.
- **Strategies package — eager imports**: `lumina_quant.strategies.__init__` no longer uses lazy `__getattr__`. All `strategies.registry` exports are imported eagerly; `register` and `GLOBAL_REGISTRY` from `core.plugin_registry` are re-exported so callers can do `from lumina_quant.strategies import register`.
- **Dual ABC (Strategy vs StrategyPlugin)**: Phase 4 owns unification. Do NOT merge the two ABCs now. Register both with explicit `interface` tags (`"event_driven"` / `"polars_batch"`).
- **Phase 1 config knobs**: `RuntimeConfig.memory.cap_gb` (default 8.0 GiB) and `RuntimeConfig.validation.golden_rtol` (default 1e-8). Golden comparisons must use `rtol=rt.validation.golden_rtol`; no hardcoded tolerances.
- **No os.environ hidden bus**: config must flow through explicit `RuntimeConfig` objects. `os.environ` access is only allowed inside `get_default_runtime_config()` and `load_runtime_config()`.

### Phase 4 architecture notes (2026-06-10)

- **Unified ExecutionModel**: `backtesting/execution_model.py` is the single source of truth for fees, slippage, spread, partial-fill liquidity cap, funding-rate payments, and leverage-based liquidation. Both `SimulatedExecutionHandler` (backtest) and `live/execution_live.py` (Phase 5) MUST import `ExecutionModel` from this module — not their own copies. The Phase 5 CI grep gate enforces this: `grep -r "ExecutionModel" live/execution_live.py` must be non-empty.
- **ExecutionModelConfig construction**: always use `ExecutionModelConfig.from_runtime(rt, mode="backtest"|"live")`. `BacktestConfigView` now stores `._rt` (the originating `RuntimeConfig`), so engine components that receive a view call `from_runtime(config._rt)`. Unit-test mock configs (plain classes with uppercase attrs) are handled by the private `_config_from_attrs()` helper in `execution_model.py` — that path is test-only. `from_config_obj` was deleted (Phase 4 DELETION-GATE closed).
- **LMT order fill rule (ratified 2026-06-10 — BINDING)**: BUY LMT fills IFF `bar_low < limit_price` (strict, not ≤). SELL LMT fills IFF `bar_high > limit_price` (strict, not ≥). Fill price = `limit_price` exactly; fee = `maker_fee_rate`; no slippage. The partial-fill liquidity cap (`max_bar_volume_ratio * bar_volume`) applies. The RNG is NOT consumed for LMT fills (consumed unconditionally for MKT to preserve legacy golden sequence). These rules are validated by `TickReplayValidator` (Phase 4.4).
- **Chunked-run RNG parity**: `SimulatedExecutionHandler.get_state()` / `set_state()` saves and restores `execution_model._rng` state under key `"execution_model_rng_state"`. Without this checkpoint each chunk re-seeds the RNG causing fill-price divergence. Any new fill-RNG-bearing handler MUST include a matching state key.
- **BacktestConfigView (PERMANENT ENGINE-PRIVATE ADAPTER — Phase 6, 2026-06-10)**: lives in `backtesting/_config_view.py`; re-exported from `lumina_quant.configuration` for research scripts. `Backtest()` auto-wraps a bare `RuntimeConfig` in `BacktestConfigView`; `BacktestConfigView._rt` holds the source `RuntimeConfig`. ~15 research scripts depend on the uppercase-attr surface — no deletion timeline. New production code should pass `RuntimeConfig` directly; do NOT create `BacktestConfigView` manually in new production code.
- **`run_backtest_chunked` config parameter**: pass `config=` explicitly in all optimizer / walk-forward call sites (Phase 4.3+) to avoid relying on `get_default_runtime_config()` global state.

### Phase 3 architecture notes (2026-06-10)

- **DataCollector entry-point**: `lumina_quant.data.collector.DataCollector.from_runtime_config(rt)` is the single factory for all data-collection paths. Never call `data_collector.py` functions or `data_sync.py` sync functions directly from new code; go through `DataCollector`.
- **DataConfig schema**: `RuntimeConfig.data.kinds` (list of `ohlcv | funding | feature_points | aggtrades_tick`) is the config-only switch for data-kind selection. Invalid tokens raise `ValueError` at load time — silent drops are forbidden. Empty list falls back to the default `[ohlcv, funding, feature_points]`.
- **Driver-keyed adapter selection**: `live.exchange.driver` in the profile YAML drives `from_runtime_config()` to the correct exchange adapter (`binance_futures` → `BinanceFuturesRESTClient`, `mt5` → `MT5Exchange`, `polymarket` → `PolymarketExchange`). No special-casing outside `_build_exchange_client()`.
- **Tick collection safety axis**: `aggtrades_tick` collection (`DataCollector.collect_aggtrades_raw`) is keyless read-only public market data — it has NO order side effects and does NOT require a testnet endpoint. The Phase 0 aggTrades fixture (`BTCUSDT_1h_…parquet`) was captured from the production public REST endpoint (`fapi.binance.com`). The safety gate is `data.kinds` config: `real.yaml` excludes `aggtrades_tick` so the always-on live process never runs this path. **Do NOT gate tick collection on `is_testnet`** — that would forbid capturing validation fixtures from real market data, which is their purpose.
- **Storage root**: `DataCollector.repo` returns a `ParquetMarketDataRepository` whose root comes from `storage.market_data_parquet_path`. For raw aggTrades archives, `data.tick_path` overrides the root. Never construct `ParquetMarketDataRepository` with a manually-threaded path in new code; use `collector.repo`.
- **Profile data.kinds defaults**: `paper.yaml` = `[ohlcv, funding, feature_points, aggtrades_tick]`; `real.yaml` = `[ohlcv, funding, feature_points]`; `research.yaml` = `[ohlcv, feature_points]`.

### Phase 5 architecture notes (2026-06-10)

- **Live trading entry-point**: `lumina_quant.live.trader.LiveTrader.from_runtime_config(rt)` is the only public factory. Never construct `LiveExecutionHandler` or `OrderGateway` directly from application code.
- **Config-composable go-live gate**: `live/readiness_check.py` runs pre-flight checks driven by `RuntimeConfig.live.*`. All checks are config-conditional — no hardcoded symbol lists or leverage limits. Extend by adding new checks to the registry in `readiness_check.py`.
- **Kill-switch**: `scripts/ops/request_live_stop.py` writes an atomic stop-flag file; `LiveTrader` polls for it every heartbeat. The flag path is `RuntimeConfig.live.stop_flag_path` (default `var/live_stop.flag`). **Always-on** — never remove the polling loop.
- **Paper exchange protection**: `live/paper_exchange.py` guards all real-exchange calls when `RuntimeConfig.live.mode == "paper"`. Orders routed to the paper exchange never reach `ExchangeInterface.submit_order()` on the real adapter.
- **Structural gate (Phase 5)**: `live/execution_live.py` MUST import `ExecutionModel` and `ExecutionModelConfig` from `lumina_quant.backtesting.execution_model`. The CI gate test `tests/test_live_execution_model_gate.py` enforces this. Use `ExecutionModelConfig.from_runtime(rt, mode="live")` for production live fee/cost config.

### Phase 6 architecture notes (2026-06-10)

- **Dashboard backend — module-mode only**: All 11 dashboard routes invoke Python via `runUvPythonModuleJson(moduleName, ...args)` in `apps/dashboard_web/lib/python-runtime.ts`. There is NO snippet bridge (`runUvPythonSnippetJson` was deleted). New routes must be implemented as module-mode entry-points with `if __name__ == "__main__":` guards.
- **DashboardBridgeContractV2 (canonical)**: All route descriptors live in `src/lumina_quant/dashboard/bridge.py`. The `DashboardBridgeContractV2` dataclass is the authoritative manifest consumed by the Next.js app. Never add a hard-coded route path in the frontend; add it to `_V2_ROUTES` in `bridge.py`.
- **Cutover surface routes**: `dashboard/cutover_surfaces_service.py` handles 6 routes that share a single module via `--fn <function_name>` dispatch. Add new surfaces to `_ROUTE_DISPATCH` in that file; never create a new module per surface.
- **`configuration/views.py` deleted (Phase 6)**: `BacktestConfigView` is permanent engine-private adapter in `backtesting/_config_view.py`; re-exported from `lumina_quant.configuration`. `LiveConfigView` was inlined as `_build_live_config_namespace()` in `live/trader.py`. **Never recreate views.py**.
- **`retired_stub.py` deleted (Phase 6)**: `dashboard/retired_stub.py` (Streamlit compatibility shim) was deleted. Any reference to it in tests or scripts must be updated to the Next.js route.

### Phase 7 architecture notes (2026-06-10)

- **Deleted dead code**: `team_lanes/` (orphan artifacts), `FillModel` + `LiquidityModel` in `execution_sim.py` (Phase 4 deletion gates closed), `runUvPythonSnippetJson` in `python-runtime.ts`, `retired_stub.py` (Phase 6 gate).
- **Kept by analysis — do not delete without a full consumer audit**:
  - `alpha_zoo/` — active consumers in research and backtest paths
  - `data/hyperliquid_readonly.py` — consumers: `scripts/research/collect_hyperliquid_readonly.py`, `tests/test_hyperliquid_readonly.py`, `tests/test_collect_hyperliquid_readonly_smoke.py`
  - `data/support_inventory.py` — consumers: `scripts/build_strategy_support_inventory.py`, `scripts/collect_all_strategy_support_data.py`, `scripts/research/build_multiasset_exchange_coverage_inventory.py`, `scripts/research/refresh_final_portfolio_validation_data.py`, `scripts/research/validate_saved_incumbent_portfolio.py`, `scripts/research/validate_saved_incumbent_portfolio_continuity.py`, plus `tests/test_strategy_support_inventory.py`, `tests/test_validate_saved_incumbent_portfolio_script.py`, `tests/test_validate_saved_incumbent_portfolio_continuity_script.py`
- **Python 3.14 target**: `pyproject.toml` `target-version = "py314"`; CI PYTHON_VERSION `"3.14"` in both `ci.yml` and `private-ci.yml`. All code must be ruff-clean at py314. The 5 deprecated crate `target/` dirs (`rust_metrics`, `rust_rawfirst`, `rust_hybrid_optuna`, `rust_live_signals`, plus `c_metrics`) are gitignored / untracked — no git action needed; use `native/lumina_compute/` exclusively.
- **CI native build**: the old per-crate cargo loop was replaced by `maturin develop --release -C native/lumina_compute/`. Do not re-add cargo loops for the deprecated crates.
- **Deletion gate protocol (Phase 7)**: before deleting any module, run the trio: (1) `grep -r "ModuleName" src/ tests/ scripts/` → zero hits; (2) verify import graph; (3) run full suite green. Document the grep evidence in the commit message. If consumers exist, record in AGENTS.md and defer to team-lead.

### Config-driven UX guide

All user-visible behaviour is driven by `config.yaml` / profile YAMLs — never by CLI flags or environment variables in application code.

```
Profile YAML (configs/profiles/<name>.yaml)
  └── RuntimeConfig (lumina_quant.configuration)
        ├── RuntimeConfig.trading.*      — symbols, timeframe, leverage
        ├── RuntimeConfig.execution.*    — compute_backend, slippage_rate, fees
        ├── RuntimeConfig.live.*         — mode (paper|real), exchange.driver, stop_flag_path
        ├── RuntimeConfig.data.*         — kinds, tick_path, loaders
        ├── RuntimeConfig.storage.*      — parquet path, postgres_dsn
        ├── RuntimeConfig.memory.cap_gb  — 8 GiB default (portfolio memory guard)
        └── RuntimeConfig.validation.*   — golden_rtol (1e-8)
```

- **Profile selection**: set `LQ_CONFIG_PATH=configs/profiles/<name>.yaml` or pass `--config` to the CLI.
- **Env overrides**: `load_runtime_config()` merges `LQ__<SECTION>__<KEY>` env vars on top of the YAML. Only `get_default_runtime_config()` and `load_runtime_config()` may read `os.environ` — never in strategy or indicator code.
- **CLI**: `uv run lq backtest|optimize|live|collect --config <path>`. All other flags should map to config fields; CLI flags that duplicate config fields are deprecated.
- **No global singletons**: do not use module-level config caches. Pass `RuntimeConfig` explicitly to every factory/constructor.

### Refactoring and deletion rules

1. **Deletion trio** (mandatory before removing any module): grep `src/ tests/ scripts/` for zero hits → verify import graph → suite green. Record evidence in commit message.
2. **No second global config loader**: `lumina_quant.configuration` is the ONLY entry-point. `runtime_access.py` and `config.py` are deleted; do not resurrect them.
3. **No new views.py**: `BacktestConfigView` lives in `backtesting/_config_view.py`; `LiveConfigView` was inlined. Never add a third view.
4. **No direct ctypes / cffi**: all native access goes through `lumina_quant.compute`.
5. **No new native crates**: extend `native/lumina_compute/` only.
6. **`baseline/` is frozen**: Phase 0 golden records in `baseline/` are immutable. Re-measurements must write to `docs/perf/data/` with explicit `--output`.
7. **Alpha zoo kept**: `src/lumina_quant/alpha_zoo/` has active research consumers; do not delete without a full consumer audit.
8. **Structural gate imports**: any `# noqa: F401  # Phase N structural gate` comment documents an intentional import that enforces module-level coupling. Do not remove these imports or their comments without closing the associated gate.

### How to add a strategy (Phase 1+)

> **One template file + a config name — no framework edits.** Dropping a
> conforming module into `src/lumina_quant/strategies/` is sufficient:
> `strategies/registry._discover_plugin_strategies()` auto-imports sibling
> modules so the `@register` decorator fires, and `get_strategy_map()` /
> `resolve_strategy_class()` consult `GLOBAL_REGISTRY`. Select it by setting
> `optimization.strategy: ClassName` in config — `cli/backtest`, `cli/live`,
> and `cli/optimize` all resolve through the registry. The drop-in flow is
> gated by `tests/integration/test_registry_dropfile_e2e.py`.

1. Create `src/lumina_quant/strategies/<name>.py` with a class that subclasses `Strategy` (event-driven) or `StrategyPlugin` (polars-batch).
2. Decorate with `@register("strategy", "ClassName", interface="event_driven"|"polars_batch")` from `lumina_quant.core.plugin_registry`. This is the ONLY registration step — do **not** edit `registry.py` / `_STRATEGY_MAP` (the curated map remains for the shipped strategies but is no longer required for new ones).
3. Reference it by name in config: `optimization.strategy: ClassName`.
4. (Optional) Add a param schema entry in `src/lumina_quant/tuning/param_registry.py` if the strategy exposes tunable hyperparameters (otherwise it runs with `get_param_schema()` defaults).
5. Write at least one unit test under `tests/` verifying signals output shape and `GLOBAL_REGISTRY.get("strategy", "ClassName")` returns the class.

`MovingAverageCrossStrategy` (`strategies/moving_average.py`) is the canonical
decorator-registered example.

### How to add an indicator (Phase 1+)

1. Create `src/lumina_quant/indicators/<name>.py` implementing the indicator logic (pure Polars / NumPy; no I/O).
2. Decorate with `@register("indicator", "ClassName")` from `lumina_quant.core.plugin_registry`.
3. Re-export from `src/lumina_quant/indicators/__init__.py`.
4. Write a unit test verifying output shape and `GLOBAL_REGISTRY.get("indicator", "ClassName")` returns the class.
5. If the indicator is compute-intensive, add a matching native kernel in `native/lumina_compute/src/` and expose it via `lumina_quant.compute.indicators`.

### How to add a portfolio optimizer (Phase 1+)

1. Create `src/lumina_quant/portfolio/<name>.py` implementing the optimizer (must respect `RuntimeConfig.memory.cap_gb`).
2. Decorate with `@register("portfolio", "ClassName")` from `lumina_quant.core.plugin_registry`.
3. Re-export from `src/lumina_quant/portfolio/__init__.py`.
4. Write a unit test verifying allocation output shape and `GLOBAL_REGISTRY.get("portfolio", "ClassName")` returns the class.
5. Add a param schema entry in `src/lumina_quant/tuning/param_registry.py` if the optimizer has tunable hyperparameters.

### How to use a compute module (Phase 2+)

The single native extension is `native/lumina_compute/` (Rust via pyo3, built with maturin).  All 5 former ctypes loaders were migrated here in Phase 2.  All native symbols are accessed via `src/lumina_quant/compute/`.

- **Import path**: `from lumina_quant.compute import indicators`, `from lumina_quant.compute.ops import ...`
- **Build**: `maturin develop --release -C native/lumina_compute/` (CI gate in `.github/workflows/ci.yml`).
- **Memory budget**: read `RuntimeConfig.memory.cap_gb` and pass to `lumina_quant.core.memory_budget` helpers before allocating large GPU arrays.
- **Backend selection**: `RuntimeConfig.execution.compute_backend` (`"gpu"` | `"cpu"`). Never hardcode; read from config.
- **Golden tolerance**: `RuntimeConfig.validation.golden_rtol` (default `1e-8`). Pass as `rtol=` to `numpy.testing.assert_allclose` or equivalent.
- **Do NOT** call `ctypes.CDLL` or `cffi` directly from strategy code; go through `lumina_quant.compute` only.
- **Do NOT** add new native crates outside `native/lumina_compute/`; extend the existing pyo3 module.

### Guardrails for future agents

1. Before broad optimizer edits, read `.omx/plans/ralplan-portfolio-optimizer-integration-cleanup-20260507.md` and the latest `docs/session_handoff_*portfolio_optimizer*` file.
2. Add/extend regression tests before moving optimizer/tuner behavior.
3. Keep the outer portfolio memory guard in CLI paths and preserve RSS evidence fields.
4. Treat locked OOS as report/gate evidence; train/validation drive selection by default.
5. Prefer shared helpers under `src/lumina_quant/portfolio/` over copying stream alignment, split aliasing, or constraint logic into more scripts.
6. Prefer `run_optuna_study(...)` / `suggest_params_from_optuna_config(...)` for tunable optimization and `build_bounded_grid_combinations(...)` only for justified small enumerations.
7. Keep leader `private-main` clean before launching OMX team worktree mode. Worker worktrees are temporary evidence lanes; final merge, Lore commit, and push stay leader-owned after full verification.
<!-- LQ:TREE:END -->

<!-- gitnexus:start -->
# GitNexus — Code Intelligence

This project is indexed by GitNexus as **LuminaQuant** (50569 symbols, 72328 relationships, 300 execution flows). Use the GitNexus MCP tools to understand code, assess impact, and navigate safely.

> If any GitNexus tool warns the index is stale, run `npx gitnexus analyze` in terminal first.

## Always Do

- **MUST run impact analysis before editing any symbol.** Before modifying a function, class, or method, run `gitnexus_impact({target: "symbolName", direction: "upstream"})` and report the blast radius (direct callers, affected processes, risk level) to the user.
- **MUST run `gitnexus_detect_changes()` before committing** to verify your changes only affect expected symbols and execution flows.
- **MUST warn the user** if impact analysis returns HIGH or CRITICAL risk before proceeding with edits.
- When exploring unfamiliar code, use `gitnexus_query({query: "concept"})` to find execution flows instead of grepping. It returns process-grouped results ranked by relevance.
- When you need full context on a specific symbol — callers, callees, which execution flows it participates in — use `gitnexus_context({name: "symbolName"})`.

## Never Do

- NEVER edit a function, class, or method without first running `gitnexus_impact` on it.
- NEVER ignore HIGH or CRITICAL risk warnings from impact analysis.
- NEVER rename symbols with find-and-replace — use `gitnexus_rename` which understands the call graph.
- NEVER commit changes without running `gitnexus_detect_changes()` to check affected scope.

## Resources

| Resource | Use for |
|----------|---------|
| `gitnexus://repo/LuminaQuant/context` | Codebase overview, check index freshness |
| `gitnexus://repo/LuminaQuant/clusters` | All functional areas |
| `gitnexus://repo/LuminaQuant/processes` | All execution flows |
| `gitnexus://repo/LuminaQuant/process/{name}` | Step-by-step execution trace |

## CLI

| Task | Read this skill file |
|------|---------------------|
| Understand architecture / "How does X work?" | `.claude/skills/gitnexus/gitnexus-exploring/SKILL.md` |
| Blast radius / "What breaks if I change X?" | `.claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md` |
| Trace bugs / "Why is X failing?" | `.claude/skills/gitnexus/gitnexus-debugging/SKILL.md` |
| Rename / extract / split / refactor | `.claude/skills/gitnexus/gitnexus-refactoring/SKILL.md` |
| Tools, resources, schema reference | `.claude/skills/gitnexus/gitnexus-guide/SKILL.md` |
| Index, status, clean, wiki CLI commands | `.claude/skills/gitnexus/gitnexus-cli/SKILL.md` |

<!-- gitnexus:end -->
