[package]
name = "lumina_compute"
version = "0.2.0"
edition = "2024"

[lib]
name = "_compute"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28.3", features = ["extension-module"] }
numpy = "0.28.0"

[profile.release]
codegen-units = 1
lto = true
